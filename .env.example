# Instancia en el VPS — copiar a .env antes de levantar
INSTANCE_NAME=landing-alicia
FRONTEND_PORT=3004
BACKEND_PORT=8004

# Base de datos local (contenedor postgres)
POSTGRES_DB=landing
POSTGRES_USER=landing
POSTGRES_PASSWORD=cambiar-esto
